let numero = 101;

// Loop para imprimir os 10 primeiros números
for (let i = 0; i < 10; i++) {
  // Imprimir o número atual
  console.log(numero);
  document.write("<p>" + numero + "</p>");

  // Incrementar o número para o próximo
  numero++;
}